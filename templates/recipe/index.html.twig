{% extends 'base.html.twig' %}

{%  block title "Toutes les recettes" %}

{% block body %}
    <p>
        Lorem ipsum dolor sit amet consectetur, adipisicing elit. Vel, perspiciatis perferendis. Eos cupiditate quidem recusandae sapiente laudantium? Nisi molestias nemo ipsum aspernatur officiis repellat eaque natus, maxime placeat voluptatibus fugiat odit cupiditate provident ut, aliquam dolorem harum quos? Laboriosam eveniet vero qui! Nisi repudiandae doloribus eligendi veniam quasi beatae facere?
    </p>
    <p>
        <a class="btn btn-primary btn-sm" href="{{ path('recipe.create') }}">Créer une recette</a>
    </p>
    <table class = "table">
        <thead>
        <tr>
            <th>Titre</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {%  for recipe in recipes %}
        <tr>
            <td>
                <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">{{ recipe.title }}</a>
            </td>
            <td>
                <div class="d-flex gap-1">
                <a class="btn btn-primary btn-sm" href="{{ path('recipe.edit', {id: recipe.id}) }}">Editer</a>
                <form action="{{ path('recipe.delete', {id: recipe.id}) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-danger btn-sm" >Supprimer</button>
                </div>
                </form>
                
            </td>
        </tr>
        {%  endfor %}
    </tbody>
    </table>

{% endblock %}
